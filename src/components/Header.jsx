export default function Header() {
    return (
        <header className="app-header">
            <strong>Employee Management Dashboard</strong>

            <div>
                <span style={{ fontSize: 14, opacity: 0.8 }}>v0.1</span>
            </div>
        </header>
    );
}